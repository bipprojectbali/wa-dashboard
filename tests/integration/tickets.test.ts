// Tests split into separate files:
//   tickets.list.test.ts       — GET /api/tickets, POST /api/tickets
//   tickets.detail.test.ts     — GET /api/tickets/:id, PATCH transitions
//   tickets.attachments.test.ts — POST comments, POST evidence
